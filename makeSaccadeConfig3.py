# the purpose of this is to compute the pitch, yaw and roll values to determine the images locations
# we still get the ... focal length and K information from opencv ... ? But we compute pitch, yaw and roll values by our selves
#

def parse(opencv_out, filename):
    return None
